get:
  tags:
    - 众裁信息
  servers:
    - url: https://api.bilibili.com
  parameters:
    - name: access_key
      in: query
      description: APP登录Token
      schema:
        type: string
    - name: case_id
      in: query
      description: 案件id
      schema:
        type: string
      required: true
  security:
    - sessionData: []
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: ../components/schemas/CreditJuryCaseInfoResponse.yaml
