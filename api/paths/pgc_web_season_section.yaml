get:
  tags:
    - 剧集信息
  servers:
    - url: https://api.bilibili.com
  parameters:
    - name: season_id
      in: query
      description: 剧集ssid
      schema:
        type: integer
      required: true
  security:
    - sessionData: []
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: ../components/schemas/PgcWebSeasonSectionResponse.yaml
