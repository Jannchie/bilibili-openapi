post:
  tags:
    - 答题操作
  servers:
    - url: https://api.bilibili.com
  summary: 提交验证码
  security:
    - sessionData: []
  parameters:
    - in: query
      name: csrf
      schema:
        type: string
      required: true
      description: CSRF Token（位于 cookie）
  requestBody:
    required: true
    content:
      application/x-www-form-urlencoded:
        schema:
          $ref: ../components/schemas/CaptchaCheckRequest.yaml
  responses:
    '200':
      description: 提交结果
      content:
        application/json:
          schema:
            $ref: ../components/schemas/CaptchaCheckResponse.yaml
