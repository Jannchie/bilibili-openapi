get:
  tags:
    - 笔记详细信息
  servers:
    - url: https://api.bilibili.com
  summary: 查询公开笔记内容
  parameters:
    - in: query
      name: cvid
      schema:
        type: integer
      required: true
      description: 笔记对应的专栏 cvid
  responses:
    '200':
      description: 公开笔记内容
      content:
        application/json:
          schema:
            $ref: ../components/schemas/PublishNoteInfoResponse.yaml
