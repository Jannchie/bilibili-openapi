get:
  tags:
    - 用户
  summary: 登录用户状态数（双端）
  servers:
    - url: https://api.bilibili.com
  security:
    - sessionData: []
  parameters:
    - name: access_key
      in: query
      description: APP 登录 Token
      schema:
        type: string
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            $ref: ../components/schemas/NavStatResponse.yaml
