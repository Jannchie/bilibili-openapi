post:
  tags:
    - 点踩
  servers:
    - url: https://app.biliapi.net
  summary: 点踩视频（App 端）
  parameters:
    - in: header
      name: access_key
      schema:
        type: string
      required: true
      description: APP 登录 Token
  requestBody:
    required: true
    content:
      application/x-www-form-urlencoded:
        schema:
          $ref: ../components/schemas/ViewDislikeRequest.yaml
  responses:
    '200':
      description: 点踩结果
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ViewDislikeResponse.yaml
