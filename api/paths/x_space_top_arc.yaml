get:
  tags:
    - 用户空间相关
  servers:
    - url: https://api.bilibili.com
  summary: 查询用户置顶视频
  parameters:
    - in: query
      name: vmid
      schema:
        type: integer
      required: true
      description: 目标用户 mid
  responses:
    '200':
      description: 置顶视频信息
      content:
        application/json:
          schema:
            $ref: ../components/schemas/TopArcResponse.yaml
