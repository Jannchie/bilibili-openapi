get:
  tags:
    - xml弹幕
  summary: 获取实时弹幕2
  description: |
    效果与前者相同

    **使用deflate压缩，注意解码**
  servers:
    - url: https://comment.bilibili.com
  parameters:
    - name: cid
      in: path
      description: 视频cid
      schema:
        type: integer
      required: true
  responses:
    '200':
      description: 成功
      content:
        application/xml:
          schema:
            $ref: ../components/schemas/DanmakuList.yaml
