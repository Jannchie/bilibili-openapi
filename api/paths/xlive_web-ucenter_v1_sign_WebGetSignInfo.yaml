get:
  tags:
    - 直播签到
  summary: 本月直播签到信息
  servers:
    - url: https://api.live.bilibili.com
  security:
    - sessionData: []
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            $ref: ../components/schemas/WebGetSignInfoResponse.yaml
