get:
  tags:
    - 个人中心
  summary: 查询账号实名认证状态
  servers:
    - url: https://api.bilibili.com
  security:
    - sessionData: []
  parameters:
    - name: access_key
      in: query
      description: APP 登录 Token
      schema:
        type: string
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            $ref: ../components/schemas/RealnameStatusResponse.yaml
