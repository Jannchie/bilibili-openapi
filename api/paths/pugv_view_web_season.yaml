get:
  tags:
    - 课程信息
  servers:
    - url: https://api.bilibili.com
  parameters:
    - name: season_id
      in: query
      description: 课程ssid, 与番剧ssid不互通
      schema:
        type: integer
    - name: ep_id
      in: query
      description: 课程epid, 与番剧epid不互通
      schema:
        type: integer
  security:
    - sessionData: []
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: ../components/schemas/PugvViewWebSeasonResponse.yaml
