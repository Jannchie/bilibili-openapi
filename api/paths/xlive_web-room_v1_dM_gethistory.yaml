get:
  tags:
    - 直播间
  summary: 获取直播间最近历史弹幕
  description: 该接口部分返回信息不明, 仅供参考
  servers:
    - url: https://api.live.bilibili.com
  parameters:
    - name: roomid
      in: query
      description: 直播间短 ID
      schema:
        type: integer
        format: int32
      required: true
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            $ref: ../components/schemas/GethistoryResponse.yaml
