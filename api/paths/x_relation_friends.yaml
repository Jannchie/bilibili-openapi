get:
  tags:
    - 用户关系相关
  servers:
    - url: https://api.bilibili.com
  summary: 查询互相关注明细
  parameters:
    - in: query
      name: access_key
      schema:
        type: string
      required: false
      description: APP 登录 Token
  responses:
    '200':
      description: 互相关注明细
      content:
        application/json:
          schema:
            $ref: ../components/schemas/RelationFriendsResponse.yaml
