get:
  tags:
    - 用户空间相关
  servers:
    - url: https://api.bilibili.com
  summary: 查看用户空间公告
  parameters:
    - in: query
      name: mid
      schema:
        type: integer
      required: true
      description: 目标用户 mid
  responses:
    '200':
      description: 空间公告内容
      content:
        application/json:
          schema:
            $ref: ../components/schemas/NoticeResponse.yaml
