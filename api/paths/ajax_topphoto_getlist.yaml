get:
  tags:
    - 用户空间相关
  servers:
    - url: https://space.bilibili.com
  summary: 查询可用头图列表 (Web 端)
  parameters:
    - in: query
      name: mid
      schema:
        type: integer
      required: true
      description: 目标用户 mid
  responses:
    '200':
      description: 可用头图列表
      content:
        application/json:
          schema:
            $ref: ../components/schemas/TopPhotoListResponse.yaml
