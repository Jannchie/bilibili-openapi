get:
  tags:
    - 登录
  summary: 获取公钥&盐 (web 端-旧版)
  servers:
    - url: https://passport.bilibili.com
  parameters:
    - name: act
      in: query
      description: act
      schema:
        type: string
        default: getkey
      required: true
  responses:
    '200':
      description: 成功
      content:
        application/json:
          schema:
            $ref: ../components/schemas/OldWebKeyResponse.yaml
