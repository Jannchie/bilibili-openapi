type: object
properties:
  code:
    type: integer
    description: >-
      返回值，0：成功，65530：token 错误（登录错误），1：错误，60009：分区不存在，60024:
      目标分区需要人脸认证，60013：非常抱歉，您所在的地区受实名认证限制无法开播
  msg:
    type: string
    description: 错误信息
  message:
    type: string
    description: 错误信息
  data:
    type: object
    properties:
      change:
        type: integer
        format: int32
        description: 是否改变状态
      status:
        type: string
        description: LIVE
      room_type:
        type: integer
        format: int32
        description: '0'
      rtmp:
        type: object
        properties:
          addr:
            type: string
            description: RTMP 推流（发送）地址
          code:
            type: string
            description: RTMP 推流参数（密钥）
          new_link:
            type: string
            description: 获取 CDN 推流 IP 地址重定向信息的 URL
          provider:
            type: string
            description: ？？？
      protocols:
        type: array
        description: ？？？
        items:
          type: object
          properties:
            protocol:
              type: string
              description: rtmp
            addr:
              type: string
              description: RTMP 推流（发送）地址
            code:
              type: string
              description: RTMP 推流参数（密钥）
            new_link:
              type: string
              description: 获取 CDN 推流 IP 地址重定向信息的 URL
            provider:
              type: string
              description: txy
      try_time:
        type: string
        description: ？？？
      live_key:
        type: string
        description: ？？？
      notice:
        type: object
        properties:
          type:
            type: integer
            format: int32
            description: '1'
          status:
            type: integer
            format: int32
            description: '0'
          title:
            type: string
            description: 空
          msg:
            type: string
            description: 空
          button_text:
            type: string
            description: 空
          button_url:
            type: string
            description: 空
