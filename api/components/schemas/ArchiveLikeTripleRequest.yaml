type: object
required:
  - csrf
properties:
  aid:
    type: integer
    description: 稿件 avid
  bvid:
    type: string
    description: 稿件 bvid
  csrf:
    type: string
    description: CSRF Token（位于 Cookie）
