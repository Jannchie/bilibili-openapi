type: object
properties:
  code:
    type: integer
    description: 返回值，0：成功
  data:
    type: object
    properties:
      card:
        type: object
        properties:
          activity_infos:
            type: object
            description: 该条动态参与的活动
          card:
            type: string
            description: 动态详细信息
          desc:
            type: object
            description: 动态相关信息
          display:
            type: object
            description: 动态部分的可操作项
          extend_json:
            type: string
            description: 动态扩展项
      result:
        type: integer
        description: 作用尚不明确
      _gt_:
        type: integer
        description: 作用尚不明确
  message:
    type: string
    description: 错误信息
  msg:
    type: number
    description: 空
