type: object
properties:
  code:
    type: integer
    description: 返回值
  message:
    type: string
    description: 错误信息
  ttl:
    type: integer
    default: 1
  data:
    type: object
    properties:
      prompt:
        type: boolean
        description: 是否为未关注用户收藏?
      ga_data:
        type: object
      toast_msg:
        type: string
      success_num:
        type: integer
        default: 0
